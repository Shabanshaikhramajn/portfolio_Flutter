import 'package:flutter/material.dart';

class AppColors {
  // ---------------------------
  // PRIMARY COLORS
  // ---------------------------
  static const Color primary = Colors.blueAccent;
  static const Color primaryDark = Color(0xFF0039CB);
  static const Color secondary = Color(0xFF00C853);      // Green Accent
  static const Color accent = Color(0xFFFFD740);         // Yellow Accent

  // ---------------------------
  // BACKGROUND COLORS
  // ---------------------------
  static const Color background = Color(0xFF0F0F0F);      // Deep Black/Charcoal
  static const Color cardBackground = Color(0xFF1A1A1A);

  // ---------------------------
  // TEXT COLORS
  // ---------------------------
  static const Color whiteColor = Colors.white;
  static const Color blackColor = Colors.black;

  // ---------------------------
  // BORDER / DIVIDER / SHADOWS
  // ---------------------------
  static const Color border = Color(0xFF2D2D2D);
  static const Color shadow = Colors.black26;

  // ---------------------------
  // NAVIGATION BAR COLORS
  // ---------------------------
  static const Color navBackground = Color(0xFF141414);
  static const Color navHover = Color(0xFF2962FF);

  // ---------------------------
  // BUTTON COLORS
  // ---------------------------
  static const Color buttonPrimary = Color(0xFF2962FF);
  static const Color buttonHover = Color(0xFF0039CB);
}
